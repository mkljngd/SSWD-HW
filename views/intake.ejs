<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <title>Patient Intake</title>
    <style>
      .top-half-bg {
        background-color: #253e48;
      }
    </style>
  </head>
  <body class="bg-gray-100">
    <div class="top-half-bg flex justify-center items-center h-1/3 w-full">
      <h2 class="text-3xl font-bold text-white mt-4 text-center">
        Patient Intake Form
      </h2>
    </div>

    <div
      class="container max-w-lg mx-auto bg-white mt-8 p-6 rounded-t-3xl shadow-lg mb-16"
    >
      <form action="/intake" method="POST" class="space-y-6">
        <input type="hidden" name="patientId" value="<%= patientId %>" />

        <div class="mb-4">
          <label class="block text-gray-700 font-semibold">First Name:</label>
          <input
            type="text"
            name="firstName"
            class="w-full p-2 border border-gray-300 rounded-lg shadow-md"
            value="<%= patient.first_name || '' %>"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-gray-700 font-semibold">Middle Name:</label>
          <input
            type="text"
            name="middleName"
            class="w-full p-2 border border-gray-300 rounded-lg shadow-md"
            value="<%= patient.middle_name || '' %>"
          />
        </div>

        <div class="mb-4">
          <label class="block text-gray-700 font-semibold">Last Name:</label>
          <input
            type="text"
            name="lastName"
            class="w-full p-2 border border-gray-300 rounded-lg shadow-md"
            value="<%= patient.last_name || '' %>"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-gray-700 font-semibold"
            >Mobile Number:</label
          >
          <input
            type="text"
            name="mobile"
            class="w-full p-2 border border-gray-300 rounded-lg shadow-md"
            value="<%= patient.mobile || '' %>"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-gray-700 font-semibold"
            >Email Address:</label
          >
          <input
            type="email"
            name="email"
            class="w-full p-2 border border-gray-300 rounded-lg shadow-md"
            value="<%= patient.email || '' %>"
            required
          />
        </div>

        <div class="mb-4">
          <label class="block text-gray-700 font-semibold"
            >Mailing Address:</label
          >
          <textarea
            name="address"
            class="w-full p-2 border border-gray-300 rounded-lg shadow-md"
            required
          >
<%= patient.address || '' %></textarea
          >
        </div>

        <div class="text-center flex justify-between mt-6">
          <a
            href="/"
            class="bg-gray-500 text-white px-6 py-3 rounded-full shadow-lg hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-500"
            >Back</a
          >
          <button
            type="submit"
            class="bg-orange-500 text-white px-6 py-3 rounded-full shadow-lg hover:bg-orange-600 focus:outline-none focus:ring-2 focus:ring-orange-500"
          >
            Next
          </button>
        </div>
      </form>
    </div>

    <div class="mb-16"></div>
  </body>
</html>
